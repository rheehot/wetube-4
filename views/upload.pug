extends layouts/main

block content
    .form-container
        form(action=`/videos${routes.upload}`, method="post")
            label(for="file") Video File
            input(type="file", id="file", name="file" requited=true)
            input(type="text", placeholder="Title", name="title" requited=true)
            textarea(name="description", placeholder="Description" requited=true)
            input(type="submit", value="Upload Video")
